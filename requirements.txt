# ─── Core ────────────────────────────────────────────────────
pydantic>=2.7.0          # Validación de esquemas y salida estructurada de IA
httpx>=0.27.0            # Cliente HTTP async/sync para llamadas al proxy Lattice
python-dotenv>=1.0.0    # Carga de variables de entorno desde .env

# ─── Desarrollo y Tests ───────────────────────────────────────
pytest>=8.2.0            # Framework de tests
pytest-httpx>=0.30.0    # Mock de llamadas httpx en tests
